# Supabase (БД + Auth + Storage + Realtime) — см. CLAUDE.md, раздел 4
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Prisma — connection pooling (pgbouncer) + прямое подключение для миграций
DATABASE_URL=
DIRECT_URL=

# Claude API — распознавание фото холодильника (vision) + опциональные текстовые подсказки рецептов
ANTHROPIC_API_KEY=

# Лимит вызовов Claude Vision API в день на household (см. CLAUDE.md, раздел 7, пункт 4 "Монетизация")
VISION_DAILY_LIMIT=20
